name: test_belajar_bloc
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.0
  http: any
  url_launcher: any
  built_collection: ^4.3.2
  built_value: ^7.1.0
  rxdart: any

dev_dependencies:
  build_runner: any
  built_value_generator: any
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true